{
  "name": "aisle-to-islands-email-api",
  "version": "1.0.0",
  "description": "Email auto-responder API for Aisle to Islands inquiry form",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "resend": "^2.1.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
